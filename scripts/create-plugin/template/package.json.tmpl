{
  "name": "__SLUG__-plugin-project",
  "private": true,
  "version": "0.1.0",
  "description": "__NAME__ plugin — scaffolded by create-mysmb-plugin.",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix harness run dev",
    "dev:server": "npm --prefix harness run dev:server",
    "build:plugin-elements": "npm --prefix plugin/widget-elements run build",
    "validate": "echo 'Run scripts/validate.ts from the marketplace repo against ./plugin'"
  },
  "workspaces": [
    "harness",
    "plugin/widget-elements"
  ]
}

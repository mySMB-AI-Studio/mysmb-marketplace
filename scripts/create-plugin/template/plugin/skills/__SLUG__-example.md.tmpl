---
name: __SLUG__-example
description: Example skill for the __NAME__ plugin. Replace the trigger phrases below with the actual situations the model should match against.
---

# Example skill

Replace this body with imperative instructions telling the model:

1. Which tool to call (`<__SLUG__>.<tool_name>`).
2. What inputs to gather first (resolve names to ids, confirm totals, etc.).
3. What NOT to do (never invent data, never batch-delete, etc.).

Skills are discovered by their `description`. Be specific about trigger phrases — the model uses the description to decide whether to load this skill into the active context.

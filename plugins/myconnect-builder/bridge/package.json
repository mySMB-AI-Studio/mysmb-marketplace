{
  "name": "@mysmb/myconnect-builder-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Laptop-side bridge: polls the workspace WorkQ over the external MCP endpoint as the myConnect Builder service account and drives Claude Code against the myConnect repo.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcb-bridge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
